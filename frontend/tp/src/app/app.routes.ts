import { Routes } from '@angular/router';
import { UtilisateurListComponent } from './private/utilisateur-list/utilisateur-list.component'

export const routes: Routes = [
     {
       path: 'liste-personne',
       component: UtilisateurListComponent
     }
];
